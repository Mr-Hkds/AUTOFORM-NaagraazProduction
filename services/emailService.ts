export const sendUserSuccessEmail = async (to: string, name: string, tokens: number) => {
    console.log(`Stub: Sending success email to ${to} for purchased ${tokens} tokens.`);
    return Promise.resolve();
};
